import { WordmarkOnlyCurrentColor } from "./logo-wordmark-only-current-color";

/**
 * Loupe wordmark only (logotype without icon). Re-exports for backward compatibility.
 */
export const LogotypeOnlyCurrentColor = WordmarkOnlyCurrentColor;
